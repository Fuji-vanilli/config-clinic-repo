server:
  port: 3140

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/CL-ORDER-DB

management:
  endpoints:
    web:
      exposure:
        include: *